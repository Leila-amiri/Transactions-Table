import React from 'react';
import {StyledSearchInput} from './SearchInputStyles';

const SearchInput = () => {
  return (
      <StyledSearchInput
        type="search"
        placeholder="Search"
      />
  );
};

export default SearchInput;